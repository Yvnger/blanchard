<label class="gallery__filter-checkbox checkbox__wrap" tabindex="0">
    <input type="checkbox" name="{{ name }}" id="{{ id }}" class="checkbox">
    <span class="checkbox__pin"></span>
    <span class="checkbox__label">{{ title }}</span>
</label>